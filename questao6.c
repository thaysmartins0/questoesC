#include <stdio.h>

int main(){
float raio;
float area;
    printf("informe a medida do raio da circunferencia: ");
    scanf("%f", &raio);
    area= raio*raio*3.14;
    printf("a area do circulo é: %4f", area);

}
